<%= content_for(:meta_title, t('nlt.offers.publish') ) %>

<div id="newinside" class="col-md-6 col-md-offset-3">
  <h1 id="title"><%= t("nlt.offers.publish") %></h1>
  <%= render partial: "form" %>
</div>
